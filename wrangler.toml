name = "vue-zero-app"
main = "server/index.js"
compatibility_date = "2024-01-01"

# 프로젝트 고유 포트 — 다른 프로젝트(예: malgnai-public 8787)와 충돌 방지
[dev]
port = 8004

[assets]
directory = "./app"
not_found_handling = "single-page-application"
run_worker_first = ["/api/*"]
